public class Test9 {
    public int solution(String dartResult) {
        char[] one = new char[10];
        one[10] = '0';
        one = dartResult.toCharArray();
        int answer = 0;
        int score;

        for(int i = 0; i < 9; i++) {
            if(one[i] >= '0' && one[i] <= '9'){

            }
        }

        return answer;
    }
}
